---
phase: 02-model-test-coverage
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - test/models/newspaper_test.rb
  - test/fixtures/newspapers.yml
autonomous: true
requirements: [TEST-01]
tags: [minitest, model-tests, newspaper]

must_haves:
  truths:
    - "rails test test/models/newspaper_test.rb passes with zero failures and zero errors"
    - "Newspaper test file contains a presence validation test for name"
    - "Newspaper test file contains an association reflection test for has_many :editions"
    - "newspapers.yml uses meaningful name values (not 'MyString' placeholders)"
  artifacts:
    - path: test/models/newspaper_test.rb
      provides: "Real Minitest test bodies for Newspaper model"
      contains: "def test_"
      min_lines: 15
    - path: test/fixtures/newspapers.yml
      provides: "Two fixture records with meaningful names"
      contains: "name:"
  key_links:
    - from: test/models/newspaper_test.rb
      to: app/models/newspaper.rb
      via: "Newspaper.new / Newspaper.reflect_on_association(:editions)"
      pattern: "Newspaper\\.(new|reflect_on_association)"
    - from: test/models/newspaper_test.rb
      to: test/fixtures/newspapers.yml
      via: "newspapers(:one) fixture accessor"
      pattern: "newspapers\\(:one\\)"
---

<objective>
Write real Minitest tests for the Newspaper model covering name presence validation and the has_many :editions association reflection, replacing the scaffold-generated commented stub. Update newspapers.yml to use meaningful name values.

Purpose: Satisfy TEST-01 (Newspaper model tests cover name presence validation and has_many :editions association). Establishes the model-testing pattern that plans 02-02 and 02-03 will repeat for Edition and Story.

Output: A passing newspaper_test.rb with at least two real tests and a meaningful newspapers.yml fixture file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-model-test-coverage/02-RESEARCH.md
@.planning/phases/02-model-test-coverage/02-PATTERNS.md
@app/models/newspaper.rb

<interfaces>
<!-- Newspaper model declarations the executor must test against. -->

From app/models/newspaper.rb:
```ruby
class Newspaper < ApplicationRecord
  has_many :editions, dependent: :destroy
  validates :name, presence: true
end
```

From test/test_helper.rb (already wired — do NOT modify):
- ActiveSupport::TestCase auto-loads all fixtures via `fixtures :all`
- Fixture accessor `newspapers(:one)` is available in every test method
- Parallelization enabled; threshold is 50 tests (not triggered here)

Verified at planning time via rails runner:
- `Newspaper.reflect_on_association(:editions).macro` => `:has_many`
- `Newspaper.new(name: nil).valid?` => `false` with error `"can't be blank"` on `:name`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update newspapers.yml fixture with meaningful name values</name>
  <files>test/fixtures/newspapers.yml</files>
  <read_first>
    - test/fixtures/newspapers.yml (current scaffold state with `MyString` placeholders)
    - test/test_helper.rb (confirm `fixtures :all` is active — do NOT modify)
    - db/schema.rb (confirm newspapers table only has `name`, `created_at`, `updated_at` — no other columns)
  </read_first>
  <behavior>
    - Fixture `one` loads with `name: "The Daily Chronicle"` and `newspapers(:one).name == "The Daily Chronicle"`
    - Fixture `two` loads with `name: "The Evening Post"` and `newspapers(:two).name == "The Evening Post"`
    - Both fixtures are valid Newspaper records (`newspapers(:one).valid? == true`)
    - Fixture keys `one` and `two` are preserved (downstream test plans reference them)
  </behavior>
  <action>
Replace the contents of `test/fixtures/newspapers.yml` with EXACTLY this content (keep the Rails documentation comment, replace placeholder values, keep keys `one` and `two`):

```yaml
# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

one:
  name: The Daily Chronicle

two:
  name: The Evening Post
```

No other columns. `created_at` / `updated_at` are auto-managed by Rails fixtures.

Fixture keys `one` and `two` MUST be preserved — Edition fixtures reference `newspaper: one` and `newspaper: two` (test/fixtures/editions.yml depends on this contract).
  </action>
  <verify>
    <automated>grep -q 'name: The Daily Chronicle' test/fixtures/newspapers.yml && grep -q 'name: The Evening Post' test/fixtures/newspapers.yml && ! grep -q 'MyString' test/fixtures/newspapers.yml && bin/rails runner -e test 'raise unless Newspaper.find_by(name: "The Daily Chronicle")' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^one:' test/fixtures/newspapers.yml` returns `1`
    - `grep -c '^two:' test/fixtures/newspapers.yml` returns `1`
    - `grep -c 'MyString' test/fixtures/newspapers.yml` returns `0`
    - `grep -c 'name: The Daily Chronicle' test/fixtures/newspapers.yml` returns `1`
    - `grep -c 'name: The Evening Post' test/fixtures/newspapers.yml` returns `1`
    - `bin/rails runner -e test 'puts Newspaper.find_by(name: "The Daily Chronicle").class'` outputs `Newspaper`
  </acceptance_criteria>
  <done>newspapers.yml contains exactly two fixture records keyed `one` and `two` with meaningful name strings; no MyString placeholders remain; test environment loads the fixture without error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write Newspaper model tests for name presence and has_many editions</name>
  <files>test/models/newspaper_test.rb</files>
  <read_first>
    - test/models/newspaper_test.rb (current commented stub)
    - app/models/newspaper.rb (source of truth for validations and associations under test)
    - test/test_helper.rb (confirm base class wiring — `< ActiveSupport::TestCase`, `fixtures :all`)
    - test/fixtures/newspapers.yml (updated in Task 1 — confirms `newspapers(:one)` returns a valid record)
    - .planning/phases/02-model-test-coverage/02-RESEARCH.md (Patterns 1 and 5)
    - .planning/phases/02-model-test-coverage/02-PATTERNS.md (Newspaper test section)
  </read_first>
  <behavior>
    - Test `test_is_invalid_without_name`: `Newspaper.new(name: nil).valid?` is false AND `errors[:name]` includes `"can't be blank"`
    - Test `test_is_valid_with_name`: `newspapers(:one).valid?` is true
    - Test `test_has_many_editions`: `Newspaper.reflect_on_association(:editions).macro == :has_many`
    - Suite-level: `bin/rails test test/models/newspaper_test.rb` exits with status 0, zero failures, zero errors
  </behavior>
  <action>
Replace the entire contents of `test/models/newspaper_test.rb` with the following. Use `test "..." do` block syntax (Rails Minitest idiom). Keep `require "test_helper"` and `class NewspaperTest < ActiveSupport::TestCase` unchanged.

```ruby
require "test_helper"

class NewspaperTest < ActiveSupport::TestCase
  test "is invalid without name" do
    newspaper = Newspaper.new(name: nil)
    assert_not newspaper.valid?
    assert_includes newspaper.errors[:name], "can't be blank"
  end

  test "is valid with name" do
    assert_predicate newspapers(:one), :valid?
  end

  test "has many editions" do
    association = Newspaper.reflect_on_association(:editions)
    assert_equal :has_many, association.macro
  end
end
```

Style notes (enforced by rubocop-rails-omakase):
- Double-quoted strings throughout
- No trailing whitespace
- No inline comments

Do NOT test `dependent: :destroy` behavior — TEST-01 calls for the has_many association; the structural reflection check satisfies it without scope creep (per RESEARCH.md Open Question 1).

Do NOT add tests for any other Newspaper behavior. Three tests only.
  </action>
  <verify>
    <automated>bin/rails test test/models/newspaper_test.rb 2>&1 | tee /tmp/np_test.out | grep -E '3 runs, [0-9]+ assertions, 0 failures, 0 errors' && grep -q 'def test_is_invalid_without_name\|test "is invalid without name"' test/models/newspaper_test.rb</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'test "is invalid without name"' test/models/newspaper_test.rb` returns `1`
    - `grep -c 'test "is valid with name"' test/models/newspaper_test.rb` returns `1`
    - `grep -c 'test "has many editions"' test/models/newspaper_test.rb` returns `1`
    - `grep -c 'reflect_on_association(:editions)' test/models/newspaper_test.rb` returns `1`
    - `grep -c "assert_includes newspaper.errors\[:name\], \"can't be blank\"" test/models/newspaper_test.rb` returns `1`
    - `grep -c '# test "the truth" do' test/models/newspaper_test.rb` returns `0` (scaffold comment removed)
    - `bin/rails test test/models/newspaper_test.rb` exits 0
    - Output of `bin/rails test test/models/newspaper_test.rb` contains the literal substring `3 runs,` and `0 failures, 0 errors`
    - `bin/rubocop test/models/newspaper_test.rb test/fixtures/newspapers.yml` exits 0
  </acceptance_criteria>
  <done>newspaper_test.rb contains exactly three test blocks (name invalid, name valid via fixture, has_many editions reflection); `bin/rails test test/models/newspaper_test.rb` reports 3 runs, 0 failures, 0 errors; rubocop passes on the test file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner → SQLite test DB | Test code creates and reads records via ActiveRecord; only affects the test database file, which is rebuilt on every `db:test:prepare`. No production data path. |
| Fixture YAML → ActiveRecord | YAML is parsed by Rails fixture loader. Source files live in the repo; no external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | test/fixtures/newspapers.yml | accept | Fixture data is checked into git; any tampering is visible in PR review. No runtime impact (test DB only). |
| T-02-01-02 | Information disclosure | test/fixtures/newspapers.yml | accept | Fixture uses fictional newspaper names ("The Daily Chronicle", "The Evening Post") — no PII, no secrets. |
| T-02-01-03 | Denial of service | rails test runner | mitigate | Test count stays well under the 50-test parallelization threshold; no fixture transactions can conflict. Phase scope is 3 model unit tests. |
</threat_model>

<verification>
After both tasks complete:

```bash
bin/rails test test/models/newspaper_test.rb
# Expected: "3 runs, N assertions, 0 failures, 0 errors"

bin/rubocop test/models/newspaper_test.rb test/fixtures/newspapers.yml
# Expected: exit 0, no offenses
```

If `bin/rails test` reports any failure or error, do NOT proceed to summary. Fix the test or fixture and re-run.
</verification>

<success_criteria>
- [ ] `test/fixtures/newspapers.yml` contains keys `one` and `two` with meaningful name strings (no `MyString`)
- [ ] `test/models/newspaper_test.rb` contains exactly three `test "..." do` blocks
- [ ] `bin/rails test test/models/newspaper_test.rb` exits 0 with `3 runs, 0 failures, 0 errors`
- [ ] `bin/rubocop test/models/newspaper_test.rb test/fixtures/newspapers.yml` exits 0
- [ ] No commented scaffold `# test "the truth"` stub remains in the test file
</success_criteria>

<output>
After completion, create `.planning/phases/02-model-test-coverage/02-01-SUMMARY.md` documenting:
- Tests added (names and what they assert)
- Fixture changes (before/after snippets)
- Test run output (`3 runs, N assertions, 0 failures, 0 errors`)
- Rubocop result
- TEST-01 marked satisfied
</output>
